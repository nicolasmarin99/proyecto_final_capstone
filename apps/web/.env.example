# La web no necesita variables de entorno.
#
# Todas las llamadas usan rutas relativas /api/..., que en desarrollo resuelve
# el proxy de vite.config.ts y en producción las reescrituras de vercel.json.
# Mantenerlo así es lo que hace que la cookie de sesión sea de primera parte.
